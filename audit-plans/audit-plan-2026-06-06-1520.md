# Implementation Plan — Codebase Audit 2026-06-06 15:20

## Overview
26 findings: 0 Critical, 2 High, 8 Medium, 16 Low. No bugs or security Highs — this cycle is dominated by optimisation/maintainability work (the run's stated emphasis). Estimated effort: ~2 S-quick-wins, then two related L items (side-effect extraction + bulk endpoint) that together remove the codebase's biggest cost and duplication problems.

## Phase 1: Critical & Security (Do First)
| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 1 | M1 covers rate-limit bucket | `src/routes/covers.js:17` | S | Change `rateLimit(60, 60000)` → `rateLimit(60, 60000, 'covers')` so all covers requests share one per-IP counter instead of one per cover key. Add a unit test asserting the bucket name is used. |
| 2 | L1 bucket footgun guard | `src/middleware/tenant.js:409–415` | S | In `rateLimit()`, when `bucket` is null and `c.req.path` differs from the registered route pattern (or simply always when bucket is null on public paths), `console.warn` once per isolate. Prevents the covers regression recurring on future parameterised routes. |
| 3 | L2 merge audit context | `src/routes/books/duplicates.js:169, 221–226` | S | Extend the `auditLog('merge','books')` detail payload with `{ canonicalId, duplicateIds, affectedOrgIds }` — affected orgs are already computed by the `org_book_selections` GROUP BY in the merge batch. |

## Phase 2: High Priority — Hot Path & Duplication
| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 4 | H2 extract shared side-effect runner | `src/routes/students/_shared.js` (new fn), `src/routes/students/sessions.js:287–317`, `src/routes/parent.js:348–359` | M | Create `runSessionSideEffects(db, env, { studentId, organizationId, yearGroup, isMarkerSession })` containing the `runSafe` + `Promise.all([streak, stats, classGoal, band])` + `evaluateRealTime` chain; return `{ newBadges, completedGoals, bandResult, streakData }`. Replace both inline copies. Keep behaviour identical (parent path passes `isMarkerSession:false` semantics it has today). Add a unit test that both routes produce the same side-effect set for an identical session. |
| 5 | H1 bulk session endpoint | `src/routes/students/sessions.js` (new `POST /:id/sessions/bulk`), `src/components/sessions/HomeReadingRegister.js:543–606`, `src/contexts/data/useSessionOperations.js` | L | Endpoint accepts `[{date, bookId, assessment, notes, location}]` (cap ~31 days), validates each via `validateSessionInput`, inserts all rows in one `db.batch()` (chunk at 100), then calls `runSessionSideEffects` **once**. Returns aggregate `{created, newBadges, completedGoals, bandUp}`. Rewrite `handleQuickRecord`'s backfill loop (incl. the two-POST marker branch at `:573–593`) to build the day list client-side and make one call per student. Depends on #4. |
| 6 | M3 incremental class goals | `src/utils/classGoalsEngine.js:254–296` | M | Replace the per-write `recalculateClassGoalProgress` full-year aggregate with incremental `UPDATE class_goals SET current = current + ?` per metric (sessions +1, pages +N; for unique-books/genre metrics either keep a cheap targeted EXISTS check or accept drift), relying on the existing 2:30 AM drift-correction cron as reconciliation. Keep the achievement-detection (pre/post null `achieved_at` diff) logic. |

## Phase 3: Performance & Scalability
| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 7 | L7 cache parent book search | `src/routes/parent.js:472–488` | S | KV-cache OpenLibrary search results keyed `ol:psearch:<normalised q>`, TTL 24h. |
| 8 | L9 parallelise ISBN verify | `src/routes/books/duplicates.js:150–160` | S | Replace the sequential for-loop with `Promise.all` over chunks of 5; keep the 20-ISBN cap and 5s timeouts. |
| 9 | L6 deterministic rate_limits cleanup | `src/middleware/tenant.js:455–465`, `src/worker.js` (scheduled) | S | Delete the `Math.random() < 0.01` branch; ensure the daily cron's DELETE covers the table (it does — just remove the per-request path). |
| 10 | L8 wonde slug pre-resolution | `src/routes/wondeAdmin.js:135` | M | Collect all candidate slugs first, fetch existing collisions in one `SELECT slug FROM organizations WHERE slug IN (…)`, resolve suffixes in JS, then batch-insert. |
| 11 | L5 demoReset badge phase | `src/services/demoReset.js:206–230` | S | Replace per-student `recalculateStats`/`evaluateRealTime`/`evaluateBatch` with the batched `processBadgesForOrg(db, DEMO_ORG_ID)` already used by the cron. |
| 12 | L4 org-list joins | `src/routes/organization.js:179–196` | M | Replace 4 correlated subqueries with LEFT JOINed `GROUP BY organization_id` derived tables; keep the pagination semantics. Owner-only — lowest urgency in this phase. |

## Phase 4: Code Quality & Tech Debt
| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 13 | M4 consolidate string matching | `src/utils/stringMatching.js`, `src/utils/titleMatching.js` (delete), `src/components/books/bookImportUtils.js`, importers: `routes/books/import.js`, `routes/books/isbn.js`, provider modules, `utils/bookDedup.js` | M | Move `normalizeTitle`/`sanitizeForSearch` + the bigram similarity into `stringMatching.js` as `similarityFuzzy`; rename the Levenshtein one `similarityLevenshtein`; delete `titleMatching.js` and the local `normalizeTitle` in `bookImportUtils.js`; update imports; add a doc comment stating which strategy serves which use-case. Keep existing thresholds per call-site (no behaviour change). Update `.claude/structure/file-map.md` + utils-services.yaml. |
| 14 | M8+L15 constants consolidation | `src/utils/constants.js`, call-sites: `badgeEngine.js:578`, `d1Provider.js:287`, `students/_shared.js:83`, `readingBandEngine.js:41`, `parent.js` (year month), `useSessionOperations.js:63`, validation ranges | M | Add `D1_BATCH_SIZE=100`, `ACADEMIC_YEAR_START_MONTH=8`, `MATCH_THRESHOLDS`, `RATE_LIMITS`, `SESSION_FIELD_RANGES`, client `SESSION_POST_BATCH_SIZE=5`. Wire the academic-year month first — band reset and parent-token year must share it. |
| 15 | M5 split auth.js | `src/routes/auth.js` → `src/routes/auth/{login,password,register}.js` + index | M | Follow the existing `students/`/`books/` sub-router convention. Pure move, no behaviour change; update file-map + routes.yaml. |
| 16 | M5 split settings.js | `src/routes/settings.js` → `src/routes/settings/{org,ai}.js` + index | M | Same convention. |
| 17 | M6 decompose FullReadingView | `src/components/sessions/FullReadingView.js:41–74`, `HomeReadingRegister.js` | M | Extract `<DateRangePanel>`, `<ReadingInputPanel>`, `<SessionHistoryTable>`; group remaining props into `dateRange`/`handlers` objects. Target ≤12 props. |
| 18 | M7 provider decision | `src/data/index.js`, `kvProvider.js`, `jsonProvider.js`, CLAUDE.md | S (decide) / M (execute) | Decide: D1-only (recommended — delete kv/json CRUD paths, keep KV for caching; `seed:local` covers dev) or explicitly scope the provider to legacy surface. Document the outcome in CLAUDE.md either way. |
| 19 | L10 error-shape cleanup | `tours.js:51,56,60`, `books.js:71`, `organization.js:118`, `wondeAdmin.js` | S | Replace ~12 raw `c.json({error}, status)` with thrown `badRequestError()`/`notFoundError()` helpers. |
| 20 | L13 celebrations hook | `src/components/sessions/HomeReadingRegister.js:62–76` | M | Extract `useSessionCelebrations()` queue hook for the 5 snackbar/celebration states. Do alongside #5's register refactor to avoid touching the file twice. |

## Phase 5: Nice-to-Haves & Hardening
| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 21 | M2 AI retention flags | `src/services/aiService.js` (+contract work) | S code / L contractual | Add `store:false` to the OpenAI request body now. Track Vertex migration + Anthropic ZDR + DPA documentation as ops tasks. |
| 22 | L11 theme tokens | `theme.js`, `StudentTimeline.js:41–48`, `ParentQRButton.js:129–142` | S | Add `theme.custom.timeline`/`custom.assessment` tokens; replace literals. |
| 23 | L12 dead exports | `src/utils/helpers.js:90,104`, `helpers.test.js` | S | Delete `sortStudentsByPriority`/`getPrioritizedStudents` + tests (or reuse in BookRecommendations if its inline copy duplicates them — check first). |
| 24 | L14 debug leftovers + test network | `hardcoverProvider.js:121,134`, aiService test setup | S | Remove 2 console.debug; stub global fetch in aiService tests so the suite stops emitting live OPTIONS calls to api.anthropic.com. |
| 25 | L16 lint warnings | `src/__tests__/unit/badgeAudit.test.js:18–19` | S | Delete the two unused imports. |
| 26 | L3 classId 404 | `src/routes/students/sessions.js:64–75` | S | Validate classId exists in-org; return 404 instead of `[]` for unknown ids. |

## Dependencies & Ordering Notes
- #4 (shared side-effect runner) **before** #5 (bulk endpoint) and ideally before #6 (class-goal change lands in one place).
- #5 and #20 both rewrite `HomeReadingRegister` — do in the same branch.
- #13 and #14 touch overlapping util imports — sequence them, don't parallelise.
- #15/#16/#17 are independent pure-refactors; safe to do opportunistically. Remember CLAUDE.md rule: update `.claude/structure/file-map.md` + YAML on any file moves, and run `./scripts/graphify-refresh.sh` after code changes.
- No DB migrations required by any item in this plan.

## Quick Wins
- **#1 covers bucket** — one line, closes the last open half of a prior High.
- **#25 + #24 + #23** — one 20-minute hygiene sweep (lint warnings, console.debug, dead exports).
- **#21 (code half)** — `store:false` is one line.
- **#9, #11** — small deletes/substitutions with existing tested alternatives.


---

## Remediation status (same day, 2026-06-06)

Implemented across five commits (28b4427, 06ece29, 6c247e0, fb2d113, 0a9c03e, 7a5106f):

- **Phase 1**: #1 covers bucket ✅, #2 bucket footgun warn ✅, #3 merge audit details ✅ (audit_log.details column existed unused — no migration needed)
- **Phase 2**: #4 runSessionSideEffects ✅, #5 bulk endpoint + register refactor ✅, #6 incremental class goals ✅ (+ bonus fix: NULL-notes rows were silently excluded from ALL class-goal aggregates — parent sessions never counted)
- **Phase 3**: #7 ✅, #8 ✅, #9 ✅, #10 ✅, #11 ✅; #12 org-list joins **wontfix with rationale** — post-indexing, correlated subqueries are page-scoped index counts; GROUP-BY-everything would read more rows
- **Phase 4**: #13 ✅ (titleMatching merged into stringMatching), #14 ✅ (ACADEMIC_YEAR_START_MONTH + D1_BATCH_LIMIT; route-local values deliberately left in place), #15 ✅ auth/ split, #16 ✅ settings/ split, #17 ✅ FullReadingView 32→11 props, #18 ✅ D1-only (user-approved; kv/jsonProvider deleted), #19 ✅, #20 celebrations hook **deferred** — the bulk-call refactor shrank both handlers enough that extraction is no longer pressing
- **Phase 5**: #21 code half ✅ (store:false); Vertex/ZDR/DPA tracked as ops work. #22 ✅, #23 ✅, #24 ✅, #25 ✅, #26 ✅
- **Struck**: L15 (useSessionOperations BATCH_SIZE) — agent misread, never existed

Verification: 2,285 tests green (20 added), lint 0/0, production build green, graphify refreshed.
