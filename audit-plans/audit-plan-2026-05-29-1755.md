# Implementation Plan — Codebase Audit 2026-05-29 17:55

## Overview
21 findings (0 Critical, 6 High, 7 Medium, 8 Low) from the 14th audit, weighted to the new parent portal + recent session/register code. All 9 prior performance findings (2026-05-13) and the 2026-05-08 AI/compliance gaps are FIXED — no carryover except one Medium (org-list subqueries). Effort: ~5 quick wins (S), ~6 mediums, 2 large. Estimated focused effort to clear High + quick-win Mediums: ~1 day.

## Phase 1: High Priority — Data Correctness, Security, Compliance (Do First)
| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 1 | H1 `last_read_date` triggers defeat v3.64.3 | `migrations/` (new file), verify against `students/sessions.js:250–259` | S | Add migration `DROP TRIGGER IF EXISTS update_student_last_read_insert; …_delete; …_update;`. App layer already maintains `last_read_date` (school-only, marker-excluded) on every path, so removal is safe. Apply local then remote. |
| 2 | H2 parent home sessions advance `last_read_date` | `src/routes/parent.js:268–278` | S | Delete the unconditional `UPDATE students SET last_read_date = MAX(...)` block from the POST core writes. If a "last read anywhere" parent indicator is wanted later, add a separate column. |
| 3 | H4 SEN/FSM/PP/DOB exposed to `readonly` on list | `src/utils/rowMappers.js:63–72`, `src/routes/students.js:66,103` | M | Add `rowToStudentMinimal(row)` excluding `senStatus,pupilPremium,ealStatus,fsm,dateOfBirth,gender,firstLanguage,ealDetailedStatus`. Use it in `GET /api/students` for non-teacher/admin roles (check `c.get('userRole')`). Keep full fields on the single-student detail view for staff only. Verify a `readonly` token cannot retrieve protected characteristics anywhere. |
| 4 | H3 rate limiter keys on concrete path | `src/middleware/tenant.js:389,408`; call sites `parent.js:79,176,339`, `covers.js:16` | S | Add optional `bucket` param: `rateLimit(max, windowMs, bucket)`. Use `const endpoint = bucket || c.req.routePath || c.req.path;` for the `rate_limits.endpoint` column. Pass explicit buckets: `'parent:view'`, `'parent:sessions'`, `'parent:books'`, `'covers'`. Add an IP-only aggregate limiter for `/api/parent/*`. |
| 5 | M4 parent GET ignores Article 18 | `src/routes/parent.js:79–105` | S | After `validateParentToken`, add `if (tokenRow.processing_restricted) return c.json({ error: 'Temporarily unavailable' }, 403);` mirroring the POST guard at `:188–193`. |

## Phase 2: Medium — Register Bugs & Child-Safety Hardening
| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 6 | M1 "+" custom-count button permanently disabled | `src/components/sessions/QuickReadingView.js:311` | S | Change `disabled={isRecording || maxMultiple < 5}` to `disabled={isRecording || maxMultiple < 2}`, OR decouple the "+" dialog from the 3-day `previousDays` window so it offers up to `multipleMaxDays` (14). Confirm the dialog `max` is no longer clamped to ≤4. |
| 7 | M2 multi-day backfill double-counts non-contiguous READ day | `src/components/sessions/HomeReadingRegister.js:538–584`; disable logic `QuickReadingView.js:189–194` | M | In `handleQuickRecord`'s backfill loop, skip any day (i>0) that already has a non-marker `home`/`school` session for that date. And/or change `unrecordedPrev` to count the *contiguous* run of NONE days immediately preceding `selectedDate` (break at first recorded day). Add a unit test for the Mon=NONE/Tue=READ/Wed=NONE → press "3" case. |
| 8 | M6 content moderation bypassed on cache hit | `src/routes/books/recommendations.js:341–381` | S | Call `filterContentSafe(enriched)` on the cache-hit branch before returning, same as the generate branch at `:532`. |
| 9 | M7 MyLogin logs children's email/name | `src/routes/mylogin.js:196,208,244,282` | S | Replace `email`/`name` in log args with `mylogin_id` or a hashed id. Verify no other route logs PII (grep `console.*` for name/email/token). |
| 10 | M8 per-session full-year class-goal rescan | `src/utils/classGoalsEngine.js:131–240,254–313` | M | Make `updateClassGoalOnSession` incremental (apply the single session's delta) OR remove the synchronous call and rely on the nightly drift cron (`worker.js:711–749`) + optimistic client increment. |
| 11 | M9 org-list correlated subqueries (carryover) | `src/routes/organization.js:179–196` | M | Replace 4 correlated subqueries + the `hasErrors` EXISTS with `LEFT JOIN (… GROUP BY organization_id)` derived tables. Add indexes listed in the report. |
| 12 | M5 AI zero-retention / DPA | `src/services/aiService.js:136–283`, ops/contracts | L | OpenAI: set `store:false`. Google: migrate to Vertex AI. Anthropic: pursue ZDR + assert. Maintain Art.28 DPAs + UK IDTA/SCC docs. (Code change is small; the contractual part is the work.) |

## Phase 3: Performance & Scalability
| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 13 | H5 session side-effect fan-out + client POST loop | `src/routes/parent.js:282–319`, `src/routes/students/sessions.js:261–302`, `src/components/sessions/HomeReadingRegister.js:499–743` | L | Wrap the four side-effects in `c.executionCtx.waitUntil(...)` so the response returns immediately (nightly cron reconciles). Add a bulk session-create endpoint (`POST /:id/sessions/bulk`) that writes N sessions and runs side-effects ONCE; switch `handleQuickRecord`/`handleRecordReading` to call it instead of looping `addReadingSession`. |
| 14 | L7 demo-reset badge phase sequential/un-hoisted | `src/services/demoReset.js:206–230` | S | Replace the per-student `recalculateStats`+`evaluateRealTime`+`evaluateBatch` loop with the batched `processBadgesForOrg(db, orgId)`; pass a hoisted genre map. |
| 15 | L8a confirm `rate_limits` index + cleanup | `src/middleware/tenant.js:449–459`, migrations | S | Verify composite index on `rate_limits(key, endpoint, created_at)`; raise cleanup probability or move public-path counters to KV-with-TTL. |
| 16 | L8b parent OpenLibrary search uncached | `src/routes/parent.js:422–438` | S | Cache popular query results in `RECOMMENDATIONS_CACHE` KV with a short TTL. |
| 17 | L8c slug-in-loop on wonde sync-all | `src/routes/wondeAdmin.js:135` | S | Fetch existing slugs once, generate uniques in-memory in a single pass before batching inserts. |

## Phase 4: Low — Hygiene & Hardening
| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 18 | L1 no unique constraint on active parent tokens | `migrations/`, `src/routes/parent.js:464–498` | S | Add partial unique index `ON parent_access_tokens(student_id, academic_year) WHERE revoked_at IS NULL`; handle the conflict in bulk-generate. |
| 19 | L2 stats "this week" no upper bound | `src/routes/students/stats.js:128` | S | Add `AND rs.session_date < :startOfNextWeek` to the this-week SUM. |
| 20 | L3 parent paths not fail-closed | `src/middleware/tenant.js:15–24` | S | Add `/api/parent/` to `FAIL_CLOSED_PATHS`. |
| 21 | L4 student PUT writes unvalidated `classId` | `src/routes/students.js` (PUT ~`:367`) | S | Validate `classId` against `classes WHERE id=? AND organization_id=?` before writing. |
| 22 | L5 user erasure dangling `parent_access_tokens.created_by` | `src/routes/users/gdpr.js` | S | Add `UPDATE parent_access_tokens SET created_by = NULL WHERE created_by = ?` to the erase batch. Update the stale "26 tables" comment in `orgPurge.js`. |
| 23 | L6 legacy `buildPrompt` raw injection | `src/services/aiService.js:62–113` | S | Grep for `generateRecommendations` callers; if none, delete the legacy path; else apply `tagUserInput`. |
| 24 | Lint warnings | `src/__tests__/unit/badgeAudit.test.js:18–19` | S | Remove or prefix-underscore the unused `evaluateRealTime`/`evaluateBatch` imports. |

## Dependencies & Ordering Notes
- **#1 and #2 must ship together** — fixing only one leaves the symptom (the trigger re-introduces it if only #2 is done; #2's redundant write persists if only #1 is done). After both, add an integration test asserting `last_read_date` is unaffected by a home/marker session.
- **#6 and #7 are coupled** — both touch the QuickReadingView ↔ HomeReadingRegister multi-day contract; do them in one PR with a shared test.
- **#13** is the big one; do after the Phase-1/2 quick wins. It changes the response contract (no `newBadges` in the immediate response if moved to `waitUntil`) — the client celebration flow must be adjusted or kept synchronous for the badge/goal results only.

## Quick Wins (S-effort, High/Medium — do first)
- #1 drop triggers, #2 remove parent last_read update, #4 rate-limiter bucket, #5 Article-18 guard, #6 "+" button threshold, #8 moderate-on-cache-hit, #9 scrub MyLogin logs.
