# Implementation Plan — Codebase Audit 2026-04-24 19:48

## Overview
23 findings across 4 High, 11 Medium, 8 Low (0 Critical). ~11–14 hours total effort; Phase 1 (~4 h) captures the hot-path fixes. Phase 2 (~5 h) covers scalability + observability. Phase 3 (~5 h) is cleanup and CI hardening.

## Phase 1: Hot-path Fixes (Do First — ~4 h)

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 1 | Session POST has no transactional wrap across 6 writes | `src/routes/students.js:1308-1373` | M | Wrap the session `INSERT` and two student `UPDATE`s (current_book_id, last_read_date) in a single `db.batch([...])`. Wrap `updateStudentStreak` / `recalculateStats` / `evaluateRealTime` / `updateClassGoalOnSession` in an outer try/catch that logs `Sentry.captureException(err, { extra: { studentId: id, sessionId } })` and still returns 201 — the session is committed and side-effects self-heal via the nightly cron. |
| 2 | Class goals GET recalculates via 6 separate COUNT queries | `src/utils/classGoalsEngine.js:144-246`, called from `src/routes/classes.js:429` | M | Rewrite `recalculateClassGoalProgress` to issue a single `SELECT` with 6 scalar subqueries in the column list (or `COUNT(CASE WHEN metric=…)`). Keep the return shape; this is a pure internal rewrite. |
| 3 | `/api/students` list has correlated subqueries | `src/routes/students.js:243-250` | M | Drop `SELECT s.*`; name needed columns. Replace the two correlated subqueries with `LEFT JOIN student_reading_stats srs ON srs.student_id = s.id` and `LEFT JOIN (SELECT student_id, COUNT(*) as badge_count FROM student_badges GROUP BY student_id) sb ON sb.student_id = s.id`. |
| 4 | AI recommendations fetches whole 18k-book catalog before sampling | `src/routes/books.js` — `generateBroadSuggestions` | M | Move the random sample into SQL: `ORDER BY RANDOM() LIMIT 100`. Add `LIMIT` on pre-filter step too. If shuffle is needed for stable-per-student variety, seed via `ABS(RANDOM() % 1000) + student_id` or similar. |
| 5 | `rateLimit` fails open for non-auth paths when D1 unreachable | `src/middleware/tenant.js:440-452` | S | Extend `sensitiveAuthPaths` list to include `'/api/auth/demo'`, `'/api/contact'`, `'/api/signup'`, `'/api/covers/'`. Keep name or rename to `costSensitivePaths`. |
| 6 | `/api/auth/mode` leaks infra flags | `src/routes/auth.js:112-128` | S | Drop `features.d1Database` and `features.kvStorage`. Return only `mode` and `ssoEnabled`. Verify frontend doesn't read the fields (grep `features\.` in src/ — previous pass showed it doesn't). |
| 7 | Register slug retry references undefined `slug`/`slugCounter` | `src/routes/auth.js:228` | S | Replace with `finalSlug = \`${finalSlug}-${crypto.randomUUID().slice(0, 4)}\`;` or delete the retry branch entirely — it's behind a feature flag and the collision chance with a random suffix is negligible. |

## Phase 2: Scalability + Observability (~5 h)

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 8 | `DataContext.reloadDataFromServer` has no AbortController | `src/contexts/DataContext.js:55-124` | S | Create `AbortController` at start of callback, pass `signal` to each of the 5 `fetchWithAuth` calls, cleanup on re-invoke. |
| 9 | `AchievementsPage` stats fetch has no AbortController | `src/components/AchievementsPage.js:37-54` | S | Mirror the HomeReadingRegister pattern. Return cleanup from useEffect. |
| 10 | `orgPurge` loops 26 DELETEs instead of batching | `src/services/orgPurge.js:94-143` | M | Pre-build `DELETE` prepared statements into an array from `DELETE_ORDER`, call once via `await db.batch(statements)`. Remove the per-iteration try/catch. If batch throws, propagate so the purge reruns rather than leaving a half-wiped org. |
| 11 | `classGoalsEngine` 6 queries lack per-query error isolation | `src/utils/classGoalsEngine.js:144-246` | M | Wrap each aggregation in try/catch; on failure preserve the prior value from `existingGoalRow` rather than overwriting with 0. Log each failure separately via Sentry. |
| 12 | R2 `put` failure silently dropped in covers route | `src/routes/covers.js` (both handlers) | S | Inside the `.catch(err => …)` on `r2.put`, call `Sentry.captureException(err, { extra: { r2Key, provider } })`. Keep the log line. |
| 13 | Badge engine Series Finisher: N+1 author COUNT loop | `src/utils/badgeEngine.js` (Series Finisher evaluator) | M | Replace the per-author loop with `SELECT author, COUNT(DISTINCT book_id) as read_count FROM reading_sessions rs JOIN books b ON rs.book_id = b.id WHERE rs.student_id = ? AND b.author IS NOT NULL GROUP BY author`. Filter results in JS. |
| 14 | Missing compound index on `reading_sessions(org, student, date)` | `migrations/0051_reading_sessions_compound_index.sql` (new) | S | `CREATE INDEX IF NOT EXISTS idx_sessions_org_student_date ON reading_sessions(organization_id, student_id, session_date DESC);` — apply remote and local. |
| 15 | 4 npm moderate vulnerabilities (hono, dompurify, postcss, uuid) | `package.json`, `package-lock.json` | S | `npm audit fix` for hono/dompurify/postcss. Manually bump `uuid` to v14 (major); verify callers in src/ via grep — uuid is only used in rate_limits id generation. Re-run test suite. |

## Phase 3: Cleanup + CI Hardening (~5 h)

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 16 | `SELECT *` in user-delete reads password_hash needlessly | `src/routes/users.js:470,475` | S | `SELECT id, role, organization_id FROM users WHERE …`. |
| 17 | `PlatformSettings` delete has no confirmation (carryover) | `src/components/PlatformSettings.js:143-173` | S | Add typed-confirm Dialog: "Deleting will disable AI for every school using this key. Type DELETE to confirm." Only delete on exact match. |
| 18 | jsPDF eagerly imported | `src/utils/statsExport.js:1`, callers in stats components | S | Convert the export function to dynamic import: `const { jsPDF } = await import('jspdf');`. Await in the click handler that triggers PDF. |
| 19 | react-joyride eagerly imported | `src/components/tour/TourProvider.js:2`, `src/App.js` | M | `const TourProvider = React.lazy(() => import('./components/tour/TourProvider'))`. Wrap root in `Suspense fallback={null}`. |
| 20 | Sentry replay + trace sample rates at 10% | `src/instrument.js:10-22` | S | Set `replaysSessionSampleRate: 0.01`, `tracesSampleRate: 0.02`. Keep `replaysOnErrorSampleRate: 1.0`. |
| 21 | `LandingPage` duplicates AuthContext storage logic | `src/contexts/AuthContext.js`, `src/components/LandingPage.js:30-56` | M | Add `loginWithDemoData(data)` to AuthContext that performs the same writes as `login()`. Replace LandingPage's direct localStorage calls with the helper. |
| 22 | 26 `console.error` in components | multiple components | S | Add `src/utils/clientLog.js` with `logError(scope, err, context)` that forwards to `Sentry.captureException(err, { tags: { scope }, extra: context })`. Codemod replacement can be incremental. |
| 23 | Unused asset files shipped | `src/assets/ImageAssets.png`, `src/assets/shelf-decoration.png`, orphaned `icon-achievements.svg` | S | `git rm` all three. Verify build still succeeds. |
| 24 | E2E Playwright job absent from CI | `.github/workflows/build.yml` | M | New job `e2e` running after `build`: install deps, build, run `npx playwright install --with-deps chromium`, then `npm run test:e2e`. Limit to PR target (not pushes to main) to control minutes. |
| 25 | Coverage report missing in CI | `.github/workflows/build.yml`, `vitest.config.mjs` | S | Add `npm run test:coverage` step. Optional: upload to Codecov for PR diff view. |
| 26 | No ESLint configuration (carryover) | new `eslint.config.js` | M | Minimal flat-config with `eslint-plugin-react-hooks` and `eslint-plugin-react`. Add `npx eslint src/ --max-warnings 0` to CI. Expect a handful of initial violations to clean up. |

## Dependencies & Ordering Notes

- Items 1, 2, 3, 4 touch high-traffic hot paths; run full test suite + manual smoke after each.
- Item 14 (compound index) is independent; can land in parallel with any other work.
- Item 15 (npm audit fix) should run after Phase 1 so dep churn doesn't collide with route fixes.
- Item 24 (E2E in CI) is a dependency for later regression signal; do it after Phase 1 lands.
- Item 26 (ESLint) will flag issues that may overlap with Phase 2 items (e.g., missing deps in the AbortController effects); run ESLint setup *after* Phase 2 to avoid double-handling.

## Quick Wins (S-effort + High/Medium — do these first within each phase)

Phase 1: #5 rate-limit list (15 min), #6 auth/mode trim (5 min), #7 slug retry (10 min) — 30 min total, all Medium severity.
Phase 2: #8 DataContext Abort (20 min), #9 AchievementsPage Abort (20 min), #12 R2 Sentry (15 min), #14 compound index (10 min), #15 npm audit fix (30 min) — ~1.5 h total.
Phase 3: #16 SELECT * trim (5 min), #17 PlatformSettings confirm (15 min), #20 Sentry sample rates (5 min), #23 asset cleanup (5 min) — 30 min total.

Target: Phase 1 this week before any feature work; Phase 2 within this month; Phase 3 as bandwidth allows.
