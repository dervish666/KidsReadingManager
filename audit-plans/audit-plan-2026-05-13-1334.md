# Implementation Plan — Performance & Process Audit 2026-05-13 13:34

## Overview
17 findings (0 Critical, 5 High, 7 Medium, 5 Low). Estimated total effort: ~8 hours. Phase 1 (quick wins + high-impact) covers the top 5 items in ~4 hours.

## Phase 1: High-Impact Quick Wins (Do First)

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 1 | H5 — Add lint to CI | `.github/workflows/build.yml` | S | Add `- name: Lint` / `run: npm run lint` step after "Check formatting" (line 33) and before "Build" (line 35). This catches `no-undef` and `rules-of-hooks` errors that currently bypass CI. |
| 2 | H4 — Cache-Control on hot-path endpoints | `src/routes/students/stats.js`, `src/routes/books.js`, `src/routes/badges.js`, `src/routes/students/sessions.js` | M | Add `c.header('Cache-Control', 'private, max-age=60, must-revalidate')` before the `return c.json(...)` on GET handlers for: stats (60–120s), books list (60s), books search (30s), badges (30s), student detail (15s). Use `private` (tenant-scoped data). |
| 3 | M5 — Add organizationId to recommendation cache key | `src/routes/books/recommendations.js:322–329` | S | Add `organizationId` to the `cacheInputs` object: `organizationId: c.get('organizationId')`. Then in `src/utils/recommendationCache.js:23–31`, add `organizationId: inputs.organizationId || ''` to the normalised JSON. |
| 4 | M6 — Dynamic import html5-qrcode | `src/components/books/BarcodeScanner.js:11` | S | Replace static import with dynamic import inside `startScanner()` function. Remove line 11 (`import { Html5Qrcode, ... }`). In the `startScanner` function body (around line 48), add: `const { Html5Qrcode, Html5QrcodeSupportedFormats } = await import('html5-qrcode');` before the `new Html5Qrcode(...)` call. |
| 5 | M1 — Batch wondeAdmin school upserts | `src/routes/wondeAdmin.js:107–146` | S | Collect all `db.prepare(...)` statements in an array inside the for-loop instead of awaiting each. After the loop, chunk the array into 100-statement batches and `await db.batch(chunk)` for each. Follow the pattern in `src/services/wondeSync.js:215–219`. Note: `generateUniqueSlug` is async and must remain in the loop; batch the resulting prepared statement. |

## Phase 2: Backend Query Optimization

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 6 | H1 — Batch badge INSERT + prefetch author counts | `src/utils/badgeEngine.js:360–418, 377–386, 480–530` | L | (a) In `evaluateBatch()`, collect all `newBadges` during the loop, then after the loop batch-insert via `db.batch([...insertStatements])`. (b) In `processBadgesForOrg()`, prefetch author book counts once per org with `SELECT b.author, rs.student_id, COUNT(DISTINCT b.id) FROM reading_sessions rs INNER JOIN books b ... WHERE rs.student_id IN (...org students...) GROUP BY b.author, rs.student_id`, build a `Map<studentId, Map<author, count>>`, and pass into `evaluateBatch()` instead of the per-student query at line 377–386. (c) Merge the duplicate `SELECT badge_id FROM student_badges WHERE student_id = ?` queries in `evaluateRealTime` (line 292) and `evaluateBatch` (line 360) into a single call earlier in the flow. |
| 7 | H2 — Stats endpoint SQL aggregation | `src/routes/students/stats.js:94–149` | L | Replace the per-row fetch + JS loop with SQL aggregation. Keep the chunked IN-clause pattern but change the query to: `SELECT COUNT(*) as total, SUM(CASE WHEN location='home' THEN 1 ELSE 0 END) as home_count, SUM(CASE WHEN location='school' THEN 1 ELSE 0 END) as school_count, SUM(CASE WHEN session_date >= ? THEN 1 ELSE 0 END) as this_week, SUM(CASE WHEN session_date >= ? AND session_date < ? THEN 1 ELSE 0 END) as last_week FROM reading_sessions WHERE student_id IN (...)`. Keep a separate query for `mostReadBooks` (GROUP BY + ORDER BY + LIMIT 5) and `studentLastReadMap` (GROUP BY student_id, MAX(session_date)). Three SQL queries replace 25,000 in-memory rows. |
| 8 | H3 — Refactor org list subqueries | `src/routes/organization.js:179–191` | M | Replace 4 correlated subqueries with LEFT JOIN + GROUP BY. Alternative: run student_count and class_count as a single grouped query `SELECT organization_id, COUNT(*) FROM students WHERE is_active=1 GROUP BY organization_id`, build a Map, then enrich the org rows in JS. The wonde_sync_log subquery (ORDER BY + LIMIT 1) is harder to JOIN — use a CTE or a separate query with `WHERE organization_id IN (...)`. |

## Phase 3: Caching & Static Assets

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 9 | M4 — Static asset Cache-Control | `src/worker.js:398–409` | S | After `const response = new Response(assetResponse.body, assetResponse);` (line 400), add: `const path = new URL(request.url).pathname; if (/\.[a-f0-9]{8}\.(js|css)$/.test(path)) { response.headers.set('Cache-Control', 'public, max-age=31536000, immutable'); } else if (path === '/' || path.endsWith('.html')) { response.headers.set('Cache-Control', 'no-cache, must-revalidate, public'); }` |
| 10 | M3 — Budget guard for class goals cron | `src/worker.js:684–711` | S | Add `if (Date.now() > deadlineMs) break;` before the inner `for (const cls of classes.results)` loop. Also consider moving class goals drift correction to its own cron trigger (e.g., `35 2 * * *`) to avoid competing with badge cron for CPU budget. |
| 11 | M2 — Batch demo reset deletes | `src/services/demoReset.js:128–135` | S | Group independent DELETE statements and run via `db.batch()`. FK-dependent deletes (e.g., `support_ticket_notes` before `support_tickets`) must stay sequential. Independent tables (`student_preferences`, `org_book_selections`, `student_badges`, `student_reading_stats`) can batch together. |

## Phase 4: DX & Tooling

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 12 | M7 — Bundle analyzer | `package.json`, `rsbuild.config.mjs` | S | `npm install -D @rsbuild/plugin-bundle-analyzer`. Add to rsbuild config with `analyze: { analyzerMode: process.env.ANALYZE ? 'server' : 'disabled' }`. Add script: `"analyze": "ANALYZE=true npm run build"`. |
| 13 | L1 — Composite badge index | `migrations/` | S | Create migration: `CREATE INDEX IF NOT EXISTS idx_student_badges_student_earned ON student_badges(student_id, earned_at DESC);` |
| 14 | L2 — Streak settings invalidation | `src/routes/settings.js` | S | When streak settings are PUT, add `await env.READING_MANAGER_KV.delete(\`org-streak-settings:${organizationId}\`)` to immediately clear the cached value. |
| 15 | L3 — List virtualization | — | — | No action needed. Pagination at 25 rows handles this. Revisit if any view exceeds 500 rows. |
| 16 | L4 — Prefetch hints for lazy chunks | `rsbuild.config.mjs` | S | Add `performance: { preload: { type: 'all-chunks' } }` to Rsbuild config to auto-inject `<link rel="prefetch">` for async chunks. Test impact on initial page weight before shipping. |
| 17 | L5 — Dev debug script | `package.json` | S | Add `"dev:debug": "node --inspect node_modules/.bin/wrangler dev"` or equivalent. Low priority. |

## Dependencies & Ordering Notes

- **#1 (lint in CI)** is independent — ship immediately.
- **#2 (Cache-Control)** and **#3 (cache key)** are independent — can parallelize.
- **#6 (badge batching)** should be done before **#10 (class goals budget guard)** since they share cron budget.
- **#7 (stats SQL)** is independent of all others.
- **#8 (org list)** is independent but low urgency (owner-only endpoint).
- **#9 (static assets)** and **#4 (html5-qrcode)** are independent frontend items — can parallelize.

## Quick Wins (S effort + High/Medium severity)
1. **#1** — Lint in CI (S, High)
2. **#3** — Cache key fix (S, Medium)
3. **#4** — Dynamic import html5-qrcode (S, Medium)
4. **#5** — Batch wondeAdmin upserts (S, Medium)
5. **#10** — Class goals budget guard (S, Medium)
