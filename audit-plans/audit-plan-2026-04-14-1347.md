# Implementation Plan — Codebase Audit 2026-04-14 13:47

## Overview
22 findings: 0 Critical, 6 High, 15 Medium, 9 Low. Seven High items fit in under 20 minutes each. Total estimated effort ~8-10 hours across all phases. 14 prior-cycle items were closed; this plan tracks what remains.

## Phase 1: Critical & Security (Do First)

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 1 | Google API key in URL query string | `src/routes/settings.js:479` | S | Replace `?key=${apiKey}` with `headers: { 'x-goog-api-key': apiKey }`. One-line change. |
| 2 | HomeReadingRegister first-fetch missing AbortController | `src/components/sessions/HomeReadingRegister.js:194-214` | S | Mirror the pattern from the second useEffect (lines 228-258): create controller, pass `{ signal: controller.signal }`, check `error.name === 'AbortError'`, return `() => controller.abort()`. |
| 3 | PlatformSettings key deletion has no confirmation | `src/components/PlatformSettings.js:143-173` | S | Add a confirmation Dialog before the DELETE. Require typed "DELETE" to confirm, matching the destructiveness of platform-level change. Use MUI Dialog + TextField. |
| 4 | `decryptSensitiveData` plaintext fallback silent | `src/utils/crypto.js:583-585` | S | Add `console.warn` and Sentry `captureMessage('Plaintext fallback in decryptSensitiveData', { extra: { dataLength: encryptedData.length } })` when the fallback fires. Do NOT remove the fallback yet — schedule a separate migration cycle after confirming zero production events. |
| 5 | Book import confirm — error.message leakage to clients | `src/routes/books.js` (around line 1909, confirmBatchImport) | S | Change `batch.forEach((b) => b.onError(error.message))` to `batch.forEach((b) => b.onError('Import batch failed. Please contact support.'))`. Keep `console.error` with real message for server logs. |
| 6 | AI resolution — raw decrypt error surfaces | `src/routes/books.js:669, 702` (platform AI key resolution + org AI config) | S | Wrap the `decryptSensitiveData` calls in try/catch. On failure, return `{ available: false, reason: 'config_error' }` to the caller and log the real error with `console.error('[AI Resolve] decrypt failed:', err.message)`. |

## Phase 2: Data Integrity & Core Correctness

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 7 | `tenantMiddleware` hits D1 on every authenticated request | `src/middleware/tenant.js:102-129` | M | Add a KV cache: key `org:${orgId}:status`, value `{ is_active, subscription_status }`, TTL 300s. Read from KV first; on miss, query D1 and write back. Invalidate from `src/routes/stripeWebhook.js` when status transitions, and from any admin route that toggles `is_active`. |
| 8 | `orgPurge` has no atomic guarantee across 26 tables | `src/services/orgPurge.js:94-143` | M | Build all DELETE statements into an array. Use `db.batch(statements)` for atomic execution. Remove the silent try/catch-and-continue — if any delete fails, throw so the caller knows to re-run. The final anonymise UPDATE should be conditional on successful batch. Check statement count stays under D1's 100-statement cap (26 + 1 data_rights_log + 1 anonymise = 28, well within). |
| 9 | `wondeSync` full sync has no concurrency lock | `src/services/wondeSync.js:200-286` (runFullSync entry) | M | Add `is_syncing` column to `organizations` table (migration). At sync start: `UPDATE organizations SET is_syncing = 1, updated_at = datetime('now') WHERE id = ? AND is_syncing = 0` — check `meta.changes`; if 0, a sync is already running, skip. Clear in `finally`. Alternative: use KV lock with 10-minute TTL. |
| 10 | Silent failure in Wonde `schoolApproved` school-details fetch | `src/routes/webhooks.js:72-77` | S | Add Sentry `captureException(err, { tags: { webhook: 'wonde', phase: 'school_details' } })` in the catch block. Add `onboarding_incomplete` flag to the org row when fetch fails so the UI can surface a retry banner (separate feature — for now just the Sentry capture). |
| 11 | Stripe webhook swallows trial-ending email failures | `src/routes/stripeWebhook.js:212-214` | S | Add Sentry capture in the catch. Optionally log to a new `email_outbox` table for retry — but simple Sentry is the minimum so ops knows. |

## Phase 3: Performance & Scalability

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 12 | Badge evaluation cron observability + chunking plan | `src/worker.js:617-643` | M | Add `const cronStart = Date.now();` at entry. Log elapsed time after each org and at end. If elapsed > 20s, break out of org loop early and log warning with remaining count. Do NOT yet refactor — this is the measurement pass; chunking comes next. |
| 13 | Badge engine author loop — N queries per student | `src/utils/badgeEngine.js:352-367` | M | Replace the loop with a single `SELECT author, COUNT(DISTINCT book_id) AS cnt FROM reading_sessions rs JOIN books b ON b.id = rs.book_id WHERE rs.student_id = ? GROUP BY author`. Filter the author-series thresholds in memory. |
| 14 | Class-goals recalc runs 6 separate queries per class | `src/utils/classGoalsEngine.js:144-246` | M | Combine into a single statement using subqueries in SELECT list. SQLite/D1 supports `SELECT (SELECT COUNT(*) FROM …) AS sessions, (SELECT COUNT(DISTINCT book_id) FROM …) AS books, …`. Keep the date-range bind parameters consistent. |
| 15 | `students` GET uses SELECT * + correlated subqueries | `src/routes/students.js:243-250` | M | Replace correlated subqueries with `LEFT JOIN student_reading_stats srs ON srs.student_id = s.id` and `LEFT JOIN (SELECT student_id, COUNT(*) AS badge_count FROM student_badges GROUP BY student_id) sb …`. Also enumerate columns instead of `s.*`. |
| 16 | Add compound index on reading_sessions | new `migrations/0051_reading_sessions_org_index.sql` | S | `CREATE INDEX IF NOT EXISTS idx_reading_sessions_org_student_date ON reading_sessions(organization_id, student_id, session_date DESC);` Apply locally + remote. |

## Phase 4: Frontend & Accessibility

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 17 | QuickReadingView TableCell keyboard access | `src/components/sessions/QuickReadingView.js:311` | S | Add `role="button"`, `tabIndex={0}`, `onKeyDown` for Enter/Space, and `aria-label={\`Edit book for ${student.name}\`}`. |
| 18 | UserManagement class save has no loading state | `src/components/UserManagement.js:706-770` | S | Add `savingClasses` state. Disable the Save button and show `<CircularProgress size={20} />` inline while pending. Show a Snackbar on success. |
| 19 | StudentDetailDrawer doesn't restore focus on close | `src/components/students/StudentDetailDrawer.js` (handler + call sites) | S | Accept `triggerRef` prop; in `onClose`, call `triggerRef.current?.focus()`. Update callers (StudentList.js, StudentCard.js, StudentTable.js) to pass a ref. |
| 20 | HomeReadingRegister refreshSessions swallows errors | `src/components/sessions/HomeReadingRegister.js:217-225` | S | Replace `.catch(() => {})` with `.catch((err) => { console.error('[HomeReading] refresh failed', err); setSessionsError('Could not refresh. Try again.'); })`. Add an `<Alert severity="warning" onClose>` above the register. |

## Phase 5: Security Hardening & Defense-in-Depth

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 21 | classes.js soft-delete UPDATE missing org_id | `src/routes/classes.js:355-358` | S | Add `AND organization_id = ?` to both UPDATEs; bind `organizationId`. Pre-check already guards; this is defense-in-depth. |
| 22 | Badge engine genres query lacks org scope | `src/utils/badgeEngine.js:71` | S | Add `WHERE organization_id = ?` and pass `organizationId` through. OR, if genres are truly platform reference data, lock down `POST /api/genres` to `requireOwner()` and document. |
| 23 | demoReset.js template-interpolated DELETE (not exploitable today) | `src/services/demoReset.js:130` | S | Add a `// SAFETY: table/where are compile-time constants from DELETE_TABLES. Adding external input here is unsafe.` comment above line 130. |
| 24 | PlatformSettings error-message leakage in UI | `src/components/PlatformSettings.js:137, 169` | S | Replace `${error.message}` with `'Please try again or check the console for details.'`. |

## Phase 6: CI/CD & Tooling

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 25 | Add ESLint with react-hooks plugin | new `.eslintrc.json`, `package.json`, `.github/workflows/build.yml` | M | Install `eslint`, `eslint-plugin-react-hooks`. Minimal config targeting the rules we most need. Add `npx eslint src/ --max-warnings 0` step in CI. Expect 50+ initial warnings; commit baseline via `// eslint-disable-next-line` then burn down. |
| 26 | Add Playwright E2E job to CI | `.github/workflows/build.yml` | M | New job that installs Playwright browsers (`npx playwright install --with-deps chromium`), runs `npm run build`, starts `wrangler dev` in background, runs `npx playwright test`. PR-only to keep cost down. |
| 27 | Add prettier --check to CI | `.github/workflows/build.yml` | S | Step: `npx prettier --check "src/**/*.js"`. Run after ESLint. |

## Phase 7: Low-severity cleanup (as bandwidth allows)

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 28 | DRY extraction | `src/routes/students.js`, `users.js`, `auth.js`, `organization.js`, `webhooks.js`, `wondeAdmin.js`, `helpers.js`, `validation.js` | M | Extract `csvRow()` to helpers, `validatePassword()` to validation, session-input validation to validation, `generateUniqueSlug()` to helpers. Carryover from 2026-04-09 #18. |
| 29 | KV cache TTL review | `src/utils/recommendationCache.js` | S | Reduce TTL from 7d to 1-2d for recommendations; keep isbnLookup at 30d (book metadata is stable). |
| 30 | PlatformSettings console.error → logger | `src/components/PlatformSettings.js:62, 84, 136, 168` | S | Optional — keep for now, revisit when adding a proper frontend logger. |

## Dependencies & Ordering Notes

- **Items 1-6 are all independent** and under 20 minutes each — these are the "knock out in one morning" batch.
- **Item 7 (tenantMiddleware cache)** requires Stripe webhook changes to invalidate; do item 11 (Sentry capture in webhook) first to ensure the existing webhook path is reliable.
- **Item 8 (orgPurge batch)** should be tested against the demo org reset flow; the same pattern could be applied to `demoReset.js` later.
- **Item 9 (wondeSync lock)** needs a DB migration — schedule with the 0051 reading_sessions index migration (item 16) as one migration file or back-to-back.
- **Item 12 (badge cron observability)** must ship before item 13/14 refactors so we can measure impact.
- **Item 25 (ESLint)** will surface a mountain of warnings; budget time for baseline disables.

## Quick Wins (S effort + High severity)

Do these first within Phase 1 — all under 20 minutes:

1. **#1** Google API key header swap — 2 min
2. **#5** Book import error generic message — 10 min
3. **#2** HomeReadingRegister AbortController — 10 min
4. **#4** Plaintext-fallback Sentry warning — 20 min
5. **#3** PlatformSettings delete confirm dialog — 15 min
6. **#6** AI resolution decrypt try/catch — 15 min
7. **#17** QuickReadingView keyboard access — 10 min (UX High)

Total for all seven: ~1.5 hours.
